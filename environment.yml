name: guipilot
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - numpy
      - opencv-python
      - Pillow
      - torch
      - torchvision
      - ultralytics
      - pydantic
      - openai
      - requests
      - PyYAML
      - matplotlib
      - scikit-learn
      - paddleocr
      - pytest
      - black
      - isort
      - flake8
      - pre-commit
